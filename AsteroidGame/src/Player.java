import java.awt.image.BufferedImage;

public class Player extends Objetos {

    public Player(double x, double y, double width, double height, BufferedImage sprite) {
        super(x, y, width, height, sprite);
    }
}
